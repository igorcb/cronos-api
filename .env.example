# Variáveis de ambiente da API Cronos

# Executa processamento de upload de forma síncrona (sem Sidekiq)
# Use apenas em dev/preview quando precisar de atualização imediata pós-upload
# Valores: '1' para síncrono; qualquer outro valor usa job assíncrono
UPLOAD_SYNC=1